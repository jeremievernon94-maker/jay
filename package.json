{
  "name": "basketball-player-tracker",
  "version": "1.0.0",
  "description": "Application for basketball coaches to manage players",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "cd frontend && npm run build",
    "install": "npm install -w frontend && npm install -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
